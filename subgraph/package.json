{
  "name": "speculate-v3-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for SpeculateCore contract",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio speculate-v3",
    "deploy:hosted": "graph deploy --product hosted-service <GITHUB_USER>/speculate-v3",
    "create:local": "graph create --node http://localhost:8020/ speculate-v3",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 speculate-v3"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}

